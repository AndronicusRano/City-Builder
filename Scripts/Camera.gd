extends Node3D

const BASE_CAMERA_MOVEMENT_SPEED = 3
const CAMERA_SENSITIVITY = 1.5

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#if Input.is_action_pressed("pan_right"):
	#	translate_object_local(Vector3(BASE_CAMERA_MOVEMENT_SPEED * delta, 0, 0))
	#if Input.is_action_pressed("pan_left"):
	#	translate_object_local(Vector3(-BASE_CAMERA_MOVEMENT_SPEED * delta, 0, 0))
	#if Input.is_action_pressed("pan_forward"):
	#	translate_object_local(Vector3(0, 0, -BASE_CAMERA_MOVEMENT_SPEED * delta))
	#if Input.is_action_pressed("pan_backward"):
	#	translate_object_local(Vector3(0, 0, BASE_CAMERA_MOVEMENT_SPEED * delta))
	#if Input.is_action_pressed("pan_up"):
	#	translate_object_local(Vector3(0, BASE_CAMERA_MOVEMENT_SPEED * delta, 0))
	#if Input.is_action_pressed("pan_down"):
	#	translate_object_local(Vector3(0, -BASE_CAMERA_MOVEMENT_SPEED * delta, 0))
	
	#if Input.is_action_pressed("begin_rotate"):
	#	rotate_object_local(Vector3(-Input.get_last_mouse_velocity().y, -Input.get_last_mouse_velocity().x, 0).normalized(), delta * CAMERA_SENSITIVITY)
	#rotation.z = 0
		
		
		
		
	
	
		
		
		
